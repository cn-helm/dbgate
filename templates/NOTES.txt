1. DbGate has been installed.

{{- if .Values.ingress.enabled }}
Open: http{{ if .Values.ingress.tls }}s{{ end }}://{{ (first .Values.ingress.hosts).host }}
{{- else }}
To access it locally:
  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "dbgate.fullname" . }} 3000:{{ .Values.service.port }}
Then open http://127.0.0.1:3000
{{- end }}
